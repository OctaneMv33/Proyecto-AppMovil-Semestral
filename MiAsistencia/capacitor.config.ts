import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'MA1.0',
  appName: 'MiAsistencia',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
